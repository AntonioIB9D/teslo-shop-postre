/* This TypeScript code snippet is defining an enum called `ValidRoles` and exporting it. Enums in
TypeScript allow you to define a set of named constants. In this case, the enum `ValidRoles` has
three members: `admin`, `superUser`, and `user`, each assigned a string value. */
export enum ValidRoles {
  admin = 'admin',
  superUser = 'super-user',
  user = 'user',
}
